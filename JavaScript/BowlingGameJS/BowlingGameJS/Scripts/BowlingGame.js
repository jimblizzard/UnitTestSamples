// BowlingGame.js
(function (window) {

    function roll(pins) {
        return; 
    };

    function score() {
        return;
    };

    window.roll = roll;
    window.score = score; 
})(window);